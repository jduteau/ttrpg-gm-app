# API base URL for the TTRPG GM server.
# Leave empty when using the Vite dev proxy (default for local development).
# Set to the full server URL for separate deployments (e.g. Netlify + Railway).
# VITE_API_URL=https://your-server.railway.app
